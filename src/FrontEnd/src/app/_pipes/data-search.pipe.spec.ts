import { DataSearchPipe } from './data-search.pipe';

describe('DataSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DataSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
