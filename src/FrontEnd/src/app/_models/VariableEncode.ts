export class VariableEncode {
    public name: string = "";
    public method: string = "";

    constructor(name: string, method: string) {
        this.name = name;
        this.method = method;
    }
}